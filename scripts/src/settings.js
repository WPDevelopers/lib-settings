/**
 * Settings
 *
 * @version 0.1.3
 * @returns {Object}
 */
define( 'lib.settings', function( require, exports, module ) {
  console.log( module.id, 'loaded' );

  return function() {
    console.log( module.id, 'initialized' );

  }

});